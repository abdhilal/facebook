{{-- <h1>{{ $post->title }}</h1>
<p>{{ $post->content }}</p>

<h3>التعليقات:</h3>
<ul>
    {{-- <img src="{{ asset('img/' . $post->media) }}" alt=""> --}}

    {{-- @foreach($post->comments as $comment)
        <li>{{ $comment->comment }}</li>
    @endforeach
</ul> --}} --}}
